/*
 * Instructions: Change the `greeting` string to use a template literal.
 */

// Change greeting to use template literals
const myName = '[NAME]';
const greeting = `Hello, my name is ${myName}`;
console.log(greeting);
